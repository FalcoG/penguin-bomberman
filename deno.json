{
  "imports": {
    "zod": "https://deno.land/x/zod/mod.ts"
  },
  "tasks": {
    "server:run": "deno run --allow-net --check packages/server/index.ts",
    "server:watch": "deno run --watch --allow-net --check packages/server/index.ts",
    "lint": "deno check packages/server/index.ts && deno check packages/shared/mod.ts"
  }
}
